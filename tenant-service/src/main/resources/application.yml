server:
  port: 8087

spring:
  application:
    name: tenant-service
  datasource:
    url: jdbc:postgresql://localhost:5432/supermarket_tenant
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.supermarket: DEBUG
